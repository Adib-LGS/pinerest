<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Welcome!</title>
    </head>
    <body>
        <h1>Pins</h1>

        {% for pin in pins %}
            <article>
                <h1>{{ pin.title }}</h1> <!--pin.title == pin.getTitle() -->
                <p>{{ pin.getDescription() }}</p>
            </article>
        {% endfor %}
       

        {% block javascripts %}{% endblock %}
    </body>
</html>
