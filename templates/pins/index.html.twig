<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Welcome!</title>
    </head>
    <body>
        <h1>All Pins</h1>

        <a href="{{ path('pins.create') }}">Create Pin</a>

    {% if pins|length > 0 %}
        {% for pin in pins %}
            <article>
                <h1><a href="{{ path('pins.show', {'id' : pin.id }) }}">{{ pin.title }}</a></h1> {#pin.title == pin.getTitle()#}
                <p>{{ pin.getDescription() }}</p>
            </article>
        {% endfor %}

    {% else %}

        <p>Sorry, there's no pins yet !</p>

    {% endif %} 

        {% block javascripts %}{% endblock %}
    </body>
</html>
